# 文档维护

路由变化更新OpenAPI和route inventory；错误变化更新error table；RQ/Redis/Outbox变化更新Topic与Schema；DDL/Model变化更新表和ER；状态变化更新状态机；环境/进程变化更新Operations；架构选择变化新增ADR。

每次更新记录日期、代码基线、证据等级和未验证范围。禁止复制旧规格中的乱码或已删除功能。提交前运行链接、JSON/OpenAPI、UTF-8、占位符和敏感信息扫描。
