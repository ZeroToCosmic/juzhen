# 内容发布状态机

发布批次、任务和结果主要位于Gateway旧逻辑。典型流程为创建批次→待处理→处理→成功/失败/取消；自动采样、链接回填和指标更新是后续步骤。外部Buffer成功与本地结果写入之间没有分布式事务，必须依靠远端ID和本地状态防重复。

批次可由管理API取消/删除；已经产生远端副作用的任务不能仅删除本地行当作回滚。

`该图为源码事实汇总；当前没有集中状态机实现。` 来源：`gateway/app.py`发布路由、`buffer_client.py`、`celery_app.py`及发布测试。
